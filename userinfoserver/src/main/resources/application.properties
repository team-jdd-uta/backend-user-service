spring.application.name=userinfoserver
mybatis.mapper-locations=classpath:db/mapper/*.xml

spring.datasource.url=${DB_URL:jdbc:mariadb://localhost:3306/uta?useUnicode=true&characterEncoding=utf8}
spring.datasource.username=${DB_USER:UTA}
spring.datasource.password=${DB_PASSWORD:1234}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
